## List Metting Rooms

List all Metting Rooms on Metting.

> Definition

```shell
POST <%= config[:api_host] %>metrics/quanshirooms
```

```javascript
Metting.dashboard.rooms();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>metrics/quanshirooms \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %>
```

```javascript
<%= config[:ex_node_init] %>

Metting.dashboard.rooms(<%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "page_count": 1,
  "page_number": 1,
  "page_size": 30,
  "total_records": 2,
  "quanshi_rooms": [
    {
      "id": "<%= config[:ex_uuid] %>",
      "room_name": "quanshi room1",
      "email": "quanshiroom1@quanshi.us",
      "account_type": "Work Email",
      "passcode": "123",
      "status": "Available",
      "messages": {
        "last_started_time": "",
        "running_time": "",
        "selected_microphone": "",
        "selected_speaker": "",
        "selected_camera": ""
      },
      "meeting_id": ""
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
page_size *optional* | The amount of records returns within a single API call. Defaults to 30. Max of 30 records. *Default: 30*
page_number *optional* | Current page number of returned records. Default to 1. *Default: 1*