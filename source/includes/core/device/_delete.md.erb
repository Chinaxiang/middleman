## Delete a H.323/SIP Device

Delete a H.323/SIP Device on your Metting account.

> Definition

```shell
POST <%= config[:api_host] %>h323/device/delete
```

```javascript
Metting.device.delete();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>h323/device/delete \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>"
```

```javascript
<%= config[:ex_node_init] %>

var device = {
    id: "<%= config[:ex_uuid] %>",
}

Metting.device.delete(device, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "<%= config[:ex_uuid] %>",
  "deleted_at": "2017-03-14T09:27:15Z"
}
```

Arguments | &nbsp;
--- | ---
id **required** | Device ID.