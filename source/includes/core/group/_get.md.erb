## Get Group Info

Get a group on Metting via ID, return the group info.

> Definition

```shell
POST <%= config[:api_host] %>group/get
```

```javascript
Metting.group.get();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>group/get \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id=<%= config[:ex_uuid] %>
```

```javascript
<%= config[:ex_node_init] %>

var group = {
    id: "<%= config[:ex_uuid] %>"
}

Metting.group.get(group, <%= config[:ex_node_res] %>);
```


> Example Response

```json
{
  "group_id": "<%= config[:ex_uuid] %>",
  "account_id": "<%= config[:ex_uuid] %>",
  "name": "Group Test",
  "total_members": 2
}
```

Arguments | &nbsp;
--- | ---
id **required** | Group ID.